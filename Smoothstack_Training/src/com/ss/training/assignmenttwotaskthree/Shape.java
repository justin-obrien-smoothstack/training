package com.ss.training.assignmenttwotaskthree;

public interface Shape {
	double calculateArea();

	void display();
	/*
	 * display() could be implemented here as a default method, but the instructions
	 * were to have each class implement each method.
	 */
}